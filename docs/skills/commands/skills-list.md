Command skill: altaika skills list

Use when an agent needs a compact index of command skills.

Best path:
1. Run `altaika skills list`.
2. Match `name` to the command you need.
3. Read `command` for help usage and `path` for the source markdown.

This is a local index of embedded command skills.
