Command skill: altaika plan

Use when you need SQL for another platform without executing it.

Best path:
1. Choose `plan ls`, `plan describe`, or `plan cat`.
2. Set `--target` to the destination dialect.
3. Inspect warnings before giving the SQL to another system.

If rendered SQL is invalid, file an issue with source path and target dialect.
