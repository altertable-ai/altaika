Command skill: altaika skills

Use when an agent needs to discover embedded CLI skills.

Best path:
1. Run `altaika skills list`.
2. Open the `path` for the command you plan to use.
3. Prefer command help when you only need quick usage.

This command lists skills only. It does not install them into an agent runtime.
