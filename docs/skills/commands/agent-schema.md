Command skill: altaika agent schema

Use when bootstrapping an agent that has never seen Altaika.

Best path:
1. Read engines, dialects, output formats, and commands.
2. Use `--compact` when you only need command discovery.
3. Cache only for the current CLI version.
4. Re-read help for commands that need examples.

If a command exists but is missing from the schema, file an issue.
