Command skill: altaika plan ls

Use when you need metadata listing SQL for another dialect.

Best path:
1. Pass the same path you would use for `altaika ls`.
2. Set `--target`.
3. Do not execute the output until the target connection is clear.

If the SQL lists the wrong scope, file an issue with the path and dialect.
