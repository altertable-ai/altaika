Command skill: altaika plan describe

Use when you need schema inspection SQL for another dialect.

Best path:
1. Pass a full table path.
2. Set `--target`.
3. Check that catalog and schema filters are preserved.

If filters are dropped, file an issue with the rendered SQL.
